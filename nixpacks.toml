[phases.setup]
nixPkgs = ["nodejs", "npm"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run preview"

[variables]
NODE_ENV = "production"

[nixpkgs]
channel = "nixpkgs-unstable"